extends ../layout/layout.pug

include ../components/box/box.pug
include ../components/h1/h1.pug

include ../modules/loginForm/loginForm.pug

block helmet
    title Sign in
    meta(name="description" content="Sign in chat.")

block content
    main
        +box({
            width: '340px',
            height: '460px',
        }).d-flex.flex-column.center
            +h1.text-center.mb-40 Sign in
            +loginForm
