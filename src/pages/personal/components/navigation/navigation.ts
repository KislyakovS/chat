import Component, { DefaultProps } from '../../../../core/component';

import template from './navigation.tmpl';

export default class Navigation extends Component<DefaultProps> {
	constructor() {
		super(template, {});
	}
}
