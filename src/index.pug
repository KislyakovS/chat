extends ./layout/layout.pug

include ./components/button/button.pug
include ./components/link/link.pug
include ./components/field/field.pug

block helmet
    title Hello, world! 👋
    meta(name="description" content="The site is transmitted hello 👋.")

block content
    main.wrapper
        +link hello world
        +button hello world
        +field('Неверный логин') login
